### Short Polling

Short polling is a simple, client-driven communication technique where a client sends repeated, frequent HTTP requests to a server at fixed intervals (e.g., every 5 seconds) to check for updates. 